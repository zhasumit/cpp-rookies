#include <iostream>
using namespace std;

int main()
{
    float b, h;
    cout << "Enter the base and height of the traingle : ";
    cin >> b >> h;
    cout << "Area = " << (b * h) / 2;
    return 0;
}